# 26 — Ampliar cliente de API do frontend

**Depende de:** 09

Substituir `apiGet` isolado por cliente com GET/POST/PUT/PATCH/DELETE, serialização, tratamento uniforme de erro e header Bearer obtido da sessão.

**Concluído quando:** serviços conseguem chamar toda a API autenticada sem duplicar `fetch`.
