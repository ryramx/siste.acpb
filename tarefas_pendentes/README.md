# Tarefas pendentes — Sistema ACPB

Cada arquivo representa uma tarefa pequena, verificável e com dependências explícitas. Execute-as preferencialmente pela numeração. Itens `FUTURO` estão fora do escopo inicial definido no PRD.

## Ordem sugerida

1. Banco e migrações (`01` a `06`)
2. Segurança, acesso e auditoria (`07` a `15`)
3. Cadastros e integrações de domínio (`16` a `25`)
4. Frontend e experiência (`26` a `34`)
5. Qualidade e operação (`35` a `39`)
6. Funcionalidades futuras (`40` a `42`)
